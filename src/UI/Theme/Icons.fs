module App.UI.Theme.Icons

// アイコン定義
let iconClasses = "inline-block align-text-bottom"
let spinner = "animate-spin h-5 w-5 text-white"

// ユーザー関連
let user = "👤"
let users = "👥"

// アクション
let edit = "✏️"
let delete = "🗑️"
let view = "👁️"
let add = "➕"
let close = "✖️"
let search = "🔍"
let settings = "⚙️"

// ナビゲーション
let home = "🏠"
let list = "📋"
let chart = "📊"
let dashboard = "📊"

// データ
let product = "📦"
let download = "⬇️"
let upload = "⬆️"

// ステータス
let check = "✓"
let warning = "⚠️"
let info = "ℹ️"
let error = "❌"
let success = "✅"
